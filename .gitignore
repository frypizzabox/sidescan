node_modules/
dist/
build/
*.log
.DS_Store

# Per-install data (SQLite DB, pidfile) — never committed
data/

# Per-install config — never committed (users create from config.example.yaml)
config.yaml

# Secrets
.env
.env.local
.env.*.local

coverage/
.nyc_output/

/test-results/
/playwright-report/
/blob-report/

.vscode/
.idea/
